# ============================================
# WareSmart — Environment Variables SAMPLE
# ============================================
# File: .env.sample
# Copy file này thành .env rồi điền giá trị thật
#   cp .env.sample .env
#
# Lưu ý: Tất cả biến phải có prefix VITE_ để Vite inject vào frontend
# Tham khảo: https://vite.dev/guide/env-and-mode.html

# ---- Backend API ----
# Địa chỉ backend ASP.NET Core (không có trailing slash)
VITE_API_BASE_URL=http://localhost:5000/api

# ---- App Config ----
# Tên hiển thị của hệ thống
VITE_APP_NAME=WareSmart
# Phiên bản app
VITE_APP_VERSION=1.0.0

# ---- Auth ----
# Thời gian token hết hạn (phút)
VITE_TOKEN_EXPIRY_MINUTES=60
